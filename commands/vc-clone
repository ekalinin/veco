
usage() {
    echo "usage: vc clone <options>"
}


cmd_default() {
    # from vc_common
    vc_exec=$(get_vc_type)
    if [ "$vc_exec" != "" ]; then
        $vc_exec clone "$@"
    fi
}


cmd_help() {
    usage
    exit 0
}

# /* vim: set filetype=sh : */
